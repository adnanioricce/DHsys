name: 'ci'
on:
  push:
    branches:      
      - development
  pull_request:
    branches:
      - development
      - master
jobs:
  build-libraries:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: build libraries        
        run: docker build . --file src/Presentation/Api/Dockerfile.ci.libraries
  build-libraries:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: build libraries        
        run: docker build . --file src/Presentation/Api/Dockerfile.ci.api
  
