<Page x:Class="UI.Views.Conta.ContaListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UI.Views.Conta"
      xmlns:vm="clr-namespace:UI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ContaListView">
    <Page.Resources>
        <DataTemplate DataType="{x:Type vm:ContaListViewModel}">
            
        </DataTemplate>
    </Page.Resources>
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="20" MaxHeight="30"/>
            <RowDefinition MinHeight="20" MaxHeight="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox x:Name="txtSearchBox" Text="{Binding Input,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <DataGrid x:Name="_dataGrid" Grid.Row="2">

        </DataGrid>
    </Grid>
</Page>
