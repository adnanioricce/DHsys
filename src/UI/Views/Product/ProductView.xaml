<Page x:Class="UI.Views.Product.ProductView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"       
      xmlns:local="clr-namespace:UI.Views.Product"
      xmlns:vm="clr-namespace:UI.ViewModels.Product"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductListView">
    <Page.Resources>
        <DataTemplate DataType="{x:Type vm:ProductListViewModel}">
            
        </DataTemplate>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0"/>
        <Button Grid.Row="1" Content="Adicionar Producto" />
        <Frame Grid.Row="2" Source="ProductListView.xaml">
            <!--<DataGrid Grid.Row="2"/>-->
        </Frame>

    </Grid>
</Page>
